# Project TODO

- [x] PDF仕様を確認し、同期MAXを同期ランクとは別の状態として扱う実装方針を整理する
- [x] 共有HTMLに同期ランク表示を切り替える設定項目を追加する
- [x] 共有HTMLで同期MAXを同期ランクとは別に`[MAX]`表記で表示する
- [x] 人格の編集UIで同期ランクと同期MAXを識別・編集できるようにする
- [x] 人格の所持欄で同期MAX状態を視覚的に識別できるようにする
- [x] 保存・再読込・共有HTMLで同期MAX状態が維持されることを確認する（CCFOLIA JSONは変更対象外として維持）
- [x] ranking tool（persona-skill-analysis）には変更を加えないことを確認する
- [x] 同期MAXの名称・共有HTML反映・保存データ再読込を検証するNodeテストを追加する
- [x] 所持欄の人格カードから同期MAXを直接切り替えられるようにする
- [x] 人格編集UIから同期MAXを直接切り替えられるようにする
- [x] 同期MAXの切替を名称の`[MAX]`表記と共有HTML表示へ即時反映する
- [x] PC幅とモバイル幅で同期MAX操作の視認性・密度・既存導線を目視確認する
- [x] UI確認で見つかった過剰な余白、重複導線、モバイル操作性の問題を必要に応じて調整する
- [x] 最終テストと差分確認後に同期MAX機能をGitHubのmainブランチへ反映する

## アイテムDB統合 — 実装前の画面案レビュー

- [x] アイテム一覧タブ、所持品選択、MEMOの導入済み所持品一覧、チャットパレット効果反映を含む画面イメージを作成する
- [x] 画面イメージをスクリーンショットとして提示し、実装前にデータ構造と操作導線の認識を確認する
- [x] 実際のLBTのヘッダー、左ナビゲーション、中央作業領域、右側MEMO・PALETTEの構造に沿ってアイテム画面案を作り直す
- [x] アイテム機能のアイコンだけは現在の提案方向を維持する

## アイテムDB統合 — 実装

- [x] DB由来のアイテム一覧データを追加し、名称・種別・タグ・効果・チャットパレット文を定義する
- [x] ビルド固有の所持アイテム（数量・MEMO表示・パレット反映）を保存・再読込できる状態へ追加する
- [x] 既存LBTの左ナビ・中央Codex・右側MEMO／PALETTE構造に沿うアイテムタブを実装する
- [x] アイテム詳細から所持リストへの導入・削除・数量調整・パレット反映切替を実装する
- [x] 導入済みアイテムをMEMOとチャットパレットに反映する
- [x] アイテム機能の自動テストとローカルUI動作を確認し、稼働中のURLを提示する

## ローカルプレビューURL — アクセス不具合

- [ ] 一時公開URLへアクセスできない原因を診断する
- [ ] アクセス確認済みの一時公開URLを再発行して提示する

## 自動ステータス登録 — ラベルのみ登録への修正

- [x] サポートパッシブ・強化の効果が数値ステータスへ誤登録される経路を特定する
- [x] 「壊し砕くと闘志」などの効果名・参照先をラベルのみへ自動登録する
- [x] 数値ステータスへの誤登録を防ぎ、CCFOLIA JSONのstatusから除外されることを検証する
- [x] 既存のラベル参照と数値ステータスの正規の自動反映を壊さないことをテストする

## 所持品カテゴリ — 出力順

- [x] 手動順指定がない場合、MEMO・PALETTEの所持品カテゴリを常に末尾へ置く
- [x] 手動順指定がある場合、所持品カテゴリを含むユーザー指定順を尊重する
- [x] 通常順と手動順の双方を自動テストで検証する

## アイテムDB — PDF正式区分への是正

- [x] PDFのアイテム一覧を全件抽出し、名称・区分・効果・価格を照合する
- [x] 自我の欠片と硝子の破片をアイテムDBから除外する
- [x] 正式なアイテム全種類だけを回復・強化などのPDF区分でDBへ登録する
- [x] アイテム一覧・MEMO・チャットパレットへの反映と件数をテストする

## GitHub main への反映

- [x] 指定リポジトリ LBTStudio/LIMBUS_BUILD_TERMINAL のローカル差分とテスト状態だけを確認する
- [x] 指定リポジトリの最新変更を main へコミット・pushする
- [x] GitHub上の main で反映結果を確認する

## GitHub main — アイテム機能差分の精査

- [x] mainのアイテムタブ導線・スクリプト読込・DB・状態・出力の各変更を照合する
- [x] GitHub Pagesでアイテムタブが実際に表示されるか検証する
- [x] 旧端末キャッシュが再利用する資産URLをv64r60へ更新し、mainへ反映する

## 所持アイテム — 個数と削除

- [x] 導入済みアイテムごとに所持個数を直接入力できるようにする
- [x] MEMOとチャットパレットの導入済みアイテム名称を`×個数`付きで出力する
- [x] 導入済みアイテムを所持リストから削除できるようにする
- [x] 個数変更・削除・保存再読込の回帰テストとUI動作確認を行う

## オリジナルアイテム — ユーザー作成データ

- [x] 公式アイテムDBと分離したオリジナルアイテムの保存形式を追加する
- [x] オリジナルアイテムの追加・編集・削除フォームをアイテムタブへ追加する
- [x] 公式・オリジナルの両方を検索・導入・所持数管理・MEMO・PALETTE出力へ統合する
- [x] 旧保存データ互換、オリジナルアイテム削除時の所持品整合性、公式DB非変更をテストする

## 同期ランク — MEMO・PALETTE出力

- [x] 同期ランクをMEMO・PALETTEへ表示する任意設定を追加する
- [x] 同期MAXの`[MAX]`表記を既存の人格名出力と両立させる
- [x] 設定の有無と旧保存データ互換を自動テストで検証する

## アイテム最大所持数

- [x] 公式アイテムごとの最大所持数をデータへ定義する
- [x] 所持数の直接入力と増減操作をアイテム上限で制限する
- [x] アイテム詳細・所持数入力に最大所持数を明示する
- [x] オリジナルアイテムの任意上限設定と既存保存データ互換を実装する
- [x] 上限到達・直接入力・MEMO／PALETTE出力の回帰テストを追加する
- [x] UI確認・全テスト後に指定GitHubリポジトリのmainへ反映する

## LBT本体 — E.G.O検索・大罪カラー・拡大表示

- [x] E.G.O検索のキーワード候補から実データに一致しない語を除外する
- [x] E.G.O本文の「回復」を検索チップとして追加する
- [x] 公式カラー対応に基づき、憂鬱をAzure、傲慢をIndigoへ修正する
- [x] 拡大表示時に上部バー・左上ブランド文字・操作ボタンがはみ出さないようにする
- [x] LBT本体の回帰テスト・拡大表示の実画面確認後、GitHub mainへ反映する

## 共有HTML — Discordリンクプレビュー

- [x] 既存共有HTMLのURL生成方式と外部クローラーが取得できる範囲を確認する
- [x] Discordなどで識別できる共有HTMLのタイトル・説明・OGPメタデータを生成する
- [x] キャラクター名・人格・同期情報を共有カードの説明へ安全に反映する
- [x] OGP画像の扱いと、公開URLが必要となる制約を利用者へ明示する
- [x] 既存共有HTML・保存データを壊さないことを検証し、GitHub mainへ反映する

## 共有カード — 人格名優先表示

- [x] OGPタイトルを共有対象の人格名が先頭になる形式へ調整する
- [x] 人格名を共有カードのタイトル内に小さく明示できる仕組みを追加する
- [x] 人格未選択時のPC名フォールバック表示を維持する
- [x] 人格名が長い場合の省略・文字化け・OGP安全性を検証する
- [x] 実画面確認後にGitHub mainへ反映する

## 操作バー — 文字切れ・はみ出し修正

- [x] ライブラリ保存を含む操作バーのラベル切れを通常・拡大表示で再現する
- [x] 上部操作群の最小幅、折返し、アイコン・ラベル間隔を修正する
- [x] 文字を切り捨てずに操作内容を読める状態をPC・狭幅で確認する
- [x] 自動テスト・実画面確認後にGitHub mainへ反映する

## 共有URL — Discordカード実表示不具合

- [x] 実際に発行された共有URLのContent-Type・HTML・OGP画像応答を検証する
- [x] Discordクローラーが取得できない外部ホストを共有発行先から除外または降格する
- [x] Discordカードを返す共有URLの発行方式とフォールバックを実装する
- [ ] Discordへの実投稿に相当する外部クローラー検証でカード要件を確認する
- [x] 既存のダウンロード・ローカルプレビューを壊さず、GitHub mainへ反映する

## 共有リンク発行 — 遅延・コピー失敗

- [x] 並列アップロードの待機時間と失敗ホストが発行完了を遅らせる経路を特定する
- [x] 非同期後のクリップボードコピー失敗を、ユーザー操作内コピーと確実な手動復旧UIへ改善する
- [x] 共有URLを生成する前に、発行先とコピー方針を明示して待機状態を改善する
- [x] 発行成功・タイムアウト・コピー拒否を自動・実画面で検証する

## 共有リンク発行 — 自動コピーと手動コピーの両立

- [x] URL確定直後に自動コピーを試行し、成否を正確に表示する
- [x] 自動コピーの成否にかかわらず、URL欄と手動コピー操作を常設する
- [x] 手動コピー失敗時にも、URL選択とCtrl+Cで復旧できる状態を維持する

## 共有URL — GitHub Pages自己完結方式

- [x] 共有対象の状態をURLへ安全に圧縮・エンコードする方式を定義する
- [x] GitHub Pages上のLBTが共有URLを読込み、キャラクターシートを復元する
- [x] 外部アップロードの待機や共有先ごとのHTML応答に依存しない発行UIへ切り替える
- [x] Discordが取得する静的OGPと、共有先で復元するプリセットデータの制約を案内する
- [x] URL長、コピー、復元、既存共有HTMLの回帰を検証する

## 共有発行 — 永続性・管理不要性・最速性の再評価

- [x] GitHub Pages自己完結URLが共有ごとの保管・期限・手動管理を必要としない範囲を公式条件と照合する
- [x] 外部匿名ホスト・時限ホスト・サーバー側ストアを、規約順守・永続性・速度・管理負担で比較する
- [x] 外部アップロードを伴わない既定共有を維持し、URL長超過時だけ確実なフォールバックを案内する
- [x] 共有スナップショットの冗長データを削減し、通常ビルドでのURL長と発行時間を再検証する
- [x] 検証後に指定GitHubリポジトリのmainへ反映する

## 共有発行 — 可逆URL短縮

- [x] 最大級の編集済み共有状態について、現在のURL長とデータ構造別の占有率を測定する
- [x] 人格DB参照を維持したまま、編集差分を短い可逆表現へ符号化する
- [x] スキル・E.G.O・所持品・カスタムアイテム・各種補正を情報欠損なく復元する
- [x] Discord等のチャットでURL扱いされる実用長への収束を、代表ケースと最大ケースで検証する
- [x] 検証後に指定GitHubリポジトリのmainへ反映する

## 共有発行 — 長文・オリジナル・記号の完全復元

- [ ] 既存DBの最長人格と別冊PDF由来人格について、共有URLからの完全復元を検証する
- [ ] DBに存在しないオリジナル人格・オリジナルアイテムを、短縮共有URLで完全復元できることを検証する
- [ ] `-`、日本語引用符、改行、角括弧、スラッシュ等の特殊記号を含む文字列の可逆往復テストを追加する
- [ ] 最大長ケース・DB外ケースの公開共有ページ表示とGitHub main反映を検証する

## 共有発行 — 1万文字級データの短縮ID共有

- [ ] 1万文字級の独自人格データに対するURL単独圧縮の限界と共有ストア要件を記録する
- [ ] 規約順守・低管理負担・Discordカード互換の短縮ID共有先を比較・選定する
- [ ] 長文データを発行し、短いIDだけをURLに含めて完全復元する方式を実装する
- [ ] 長文・特殊記号・オリジナルデータの完全復元とDiscord本文向けURL長を公開環境で検証する
- [ ] 検証後に指定GitHubリポジトリのmainへ反映する

## 共有発行 — 多数利用者向け長文HTML配信

- [ ] 数十〜百人以上の利用者を前提に、長文HTML共有の保存件数・閲覧回数・最低1〜3日保持要件を定義する
- [ ] 無料優先で公開HTMLを保存・配信できる基盤を、規約・容量・自動削除・Discord互換性で比較する
- [ ] 短いDiscord向けURLから整形済みHTMLを開ける共有発行・閲覧方式を実装する
- [ ] 長文・多数共有・保持期間・外部閲覧を公開環境で検証する
- [ ] 検証後に指定GitHubリポジトリのmainへ反映する

## 共有発行 — 請求ゼロ絶対条件

- [ ] 無料枠超過時の自動課金有無・支払い方法要件・確実な停止条件を候補ごとに確認する
- [ ] 請求リスクを排除できる公開保存・短縮リンク方式を、時限・恒久・多数利用の条件で比較する
- [ ] 支払い情報なしで利用可能な候補について、長文HTMLの発行・閲覧・Discord対応を検証する
- [ ] 請求ゼロを保証できない候補を明示的に除外し、採用方式と限界を提案する

## 共有発行 — 利用者無制約の長文HTML共有

- [ ] 発行者・閲覧者にログイン、登録、CAPTCHA、手動保存、期限管理を求めないことを共有要件として固定する
- [ ] 請求ゼロかつ利用者無制約で使える中央共有基盤を、保存量・停止条件・規約・Discord対応で比較する
- [ ] 1万文字級の整形済みHTMLを短いURLで即時発行・閲覧する中央共有APIを実装する
- [ ] 多数利用・悪用抑止・長文完全保持・Discordリンクカードを公開環境で検証する
- [ ] 検証後に指定GitHubリポジトリのmainへ反映する

## 共有発行 — 分散HTMLプレビュー基盤

- [ ] 匿名公開、短URL、整形済みHTML配信、Discord応答、請求ゼロを必須条件として整理する
- [ ] 既存の公開HTMLプレビューサービスを複数調査し、規約・CORS・保持期間・自動投稿可否を実測する
- [ ] 利用者無制約のまま複数候補へ分散発行する優先順位・失敗時フォールバックを設計する
- [ ] 有力候補で長文HTMLの公開URL・Discordリンクカード・保持を検証する
- [ ] 採用方式と実装方針をGitHub mainへ反映する

## 共有発行 — 目的起点の三視点レビュー

- [x] 長文HTML・短いDiscordリンク・利用者無制約・請求ゼロ・継続閲覧を同時に満たす目的を技術要件へ分解する
- [x] 配信基盤、情報符号化、共有体験の三専門視点で実現方式を独立に検討する
- [x] 三提案の制約・矛盾・悪用耐性・継続性を相互比較する
- [x] 最適方針と必要な実装・運用条件を提案し、承認後に実装する

## 共有発行 — 新規発行停止を排除する再検討

- [x] 中央無料枠の上限到達による新規共有停止を許容しないことを共有要件として固定する
- [x] 保存責任・発行負荷を中央へ集中させない長文共有・短縮方式を広く調査する
- [x] 利用者無制約、請求ゼロ、停止耐性、Discord互換性の観点で候補を比較検証する
- [x] 停止しない共有方針と必要な実装上の代償を提案する

## 共有URL — 最大実データ構成の公開検証

- [x] DB内最長の人格を自動選定し、全E.G.O・全公式アイテム・全支援・全強化・精神を含む状態を構築する
- [x] 最大構成の自己完結URLを生成し、Discord本文上限との関係を計測する
- [x] GitHub Pagesの共有ビューアで最大構成を復元し、E.G.O・アイテム・強化の表示を確認する
- [x] 試験URLと実測結果を提示する

## 共有URL — 最大構成を短URLから直接完全復元する再設計

- [x] Rentryの入口ページ方式が短URLからの直接完全復元を満たさない原因を、保存・リンク・閲覧の責務へ分解する
- [x] 発行者・閲覧者の無登録、無課金、無管理、Discord本文内URL、最大構成完全復元を同時に満たす候補を再発散検討する
- [x] 最大構成データを短URLから直接取得・復元する候補を実測比較する
- [x] 採用する共有アーキテクチャを決定し、目的達成に必要な実装を提示する

## 共有URL — Direct Restore Fabric 実装

- [x] Discord実用長を超える自己完結URLをTinyURLへ匿名短縮し、短URLを発行・自動コピーする
- [x] TinyURL発行失敗時に、RentryとTelegraphへ圧縮トークンを並列保存する
- [x] `share.html`が外部保存先の短いIDからトークンを安全に取得・完全復元する
- [x] 通常の自己完結URL、短縮URL、外部保存URLの手動コピーと失敗表示を既存UI導線内で両立する
- [x] 最大構成で短URLから直接LBTシートが開くことを自動テスト・GitHub Pages実機で確認する
- [x] 検証後に指定GitHubリポジトリのmainへ反映する

## 利用ガイド — 同期化とオリジナル人格

- [x] 人格の同期ランク・同期MAXを設定する操作導線と出力・共有への反映を確認する
- [x] オリジナル人格を追加・編集・保存・共有する操作導線と必須入力を確認する
- [x] 同期化とオリジナル人格データ追加の利用ガイドを作成する
- [x] UI・保存・共有の実装内容とガイドを照合し、指定GitHubリポジトリのmainへ反映する

## 利用ガイド — チュートリアル形式への簡潔化

- [x] 同期化と創作人格追加の操作に不要な技術説明・注意書きをガイドから除外する
- [x] 画面ラベルに沿った短いステップ形式へ再編集する
- [x] 簡潔化後の操作文をUIと照合し、指定GitHubリポジトリのmainへ反映する

## 共有HTML — 同期IV表記の修正

- [x] 共有HTMLに同期IVが出るデータ経路と表示箇所を特定する
- [x] 共有表示を同期0・同期00・同期000・同期MAXだけへ正規化する
- [x] 同期表示の回帰テストとGitHub Pages実機確認を行い、mainへ反映する

## 人格DB — 派生スキルの番号・条件監査

- [x] 全人格の派生候補を抽出し、二連斬のような別番号登録・派生指定漏れを一覧化する
- [x] 原典PDF・既存DB表現を照合して、派生元・派生番号・発動条件を確定する
- [x] 派生スキルのDBデータと画面表示を修正する
- [x] 回帰テスト・GitHub Pages表示で確認し、指定GitHubリポジトリのmainへ反映する

## 使い方 — 同期化と創作人格の初歩チュートリアル

- [x] 既存の使い方画面上部に、同期化と創作人格追加の初歩ガイドを配置する
- [x] 既存の画面密度に合わせて、短い段階式の操作文と導線を実装する
- [x] PC・モバイルで表示と操作導線を確認し、指定GitHubリポジトリのmainへ反映する

## 人格DB — 東部親指カポIIIIの快刀亂麻派生番号修正

- [x] 二連斬・三連撃・爆砕斬・快刀亂麻を正しいスキル1〜4、超絶猛虎殺撃乱斬をスキル4-2として再確認する
- [x] 誤登録されたスキル3〜6・スキル6-2を連番と派生表記へ修正し、基礎番号の存在を検証する監査を追加する
- [x] 自動テスト・GitHub Pages表示で修正を確認し、指定GitHubリポジトリのmainへ反映する

## 共有URL — 外部短縮サービスの中間遷移を見せない改善

- [x] TinyURLを主URLにした場合の中間遷移と、既存のLBT直接復元URLの挙動を確認する
- [x] LBT共有ページを最初の到達先に固定する長文共有経路を設計する
- [x] 共有発行時に直接復元URLを主URLとして自動コピーし、外部短縮URLを補助へ変更する
- [x] Discord実用長・完全復元・公開GitHub Pages表示を検証し、指定GitHubリポジトリのmainへ反映する

## 共有URL — コピー結果の分かりやすさ改善

- [x] 主URLが自動コピー済みであることと、Discordへ貼る対象を明確に表示する
- [x] 予備URLを常用リンクと混同しない役割説明・折り畳み表示を実装する
- [x] 共有発行・手動コピーのクリックでモーダルが誤って閉じないよう、背景クリックだけを閉鎖対象にする
- [x] モバイル幅では主URLの入力欄とコピー操作を縦積みにし、コピー操作を十分な高さで表示する
- [x] PC・モバイルの共有モーダルで表示とコピー導線を検証し、指定GitHubリポジトリのmainへ反映する

## 共有URL — 公開復元失敗

- [ ] 公開共有URLで「共有データを読み込めませんでした」と表示される条件を再現し、失敗箇所を特定する
- [ ] Rentry・Telegraph・自己完結URLの直接復元経路を修正し、復元できない共有を減らす
- [ ] 短文・長文の公開共有URLを実画面と自動テストで検証し、GitHub mainへ反映する
- [ ] 提供されたRentry ID `6yub7uok` のラッパー経由・LBT直接URL・Rentry本文を確認し、既存共有の復旧可否を検証する

## プレビュー — 即時切替と再展開

- [x] 上部のプレビュー切替が遅れる原因を特定し、描画待ちを伴わない即時表示へ改善する
- [x] プレビューを格納した状態でも、右側プレビュー領域に常時表示する操作から再展開できるようにする
- [x] PC・モバイルで格納・再展開・出力内容更新を確認し、GitHub mainへ反映する

## モバイルUI確認用URL

- [x] PCから開ける一時公開URLで、390px幅のモバイルUIを固定表示できる確認画面を用意する

## 特殊強化とプレビュー開閉タブ

- [x] 特殊強化を肉体強化は左列、精神強化は右列へ固定してグループ化する
- [x] 右端のプレビュータブを開閉どちらでも常時表示し、一つの操作で格納・展開を切り替えられるようにする
- [x] PC・モバイルの配置と開閉操作を検証し、GitHub mainへ反映する

## モバイル操作性とアイテム表記

- [x] モバイルで開いたプレビューが独立して縦スクロールできるようにする
- [x] モバイルの上部・下部ナビゲーションで主要機能へ到達しやすいラベル・配置へ整える
- [x] 所持品関連の表示名を「アイテム」へ統一する
- [x] 390px幅の実操作でスクロール・機能移動・プレビュー開閉を検証し、GitHub mainへ反映する

## サポートパッシブ・精神の出典フィルタ

- [x] サポートパッシブと精神のデータをルールブック由来・サプリメント由来へ分類する
- [x] 両一覧へ「全て／ルールブック／サプリメント」の出典フィルタを追加し、既存の検索・条件フィルタと併用可能にする
- [x] PC・モバイルで出典フィルタの件数・絞り込みを検証し、GitHub mainへ反映する

## 精神データ — サプリメント出典

- [x] 精神一覧のサプリメント出典が0件となる原因と、既存データの出典根拠を確認する
- [x] 確認済みのサプリメント由来精神データへ出典を付与し、必要な未収録データを追加する
- [x] サプリメント精神の件数・内容・フィルタ動作をPC・モバイルで検証し、GitHub mainへ反映する
- [x] ユーザー提供済みPDFだけを基準資料として、精神データの出典・名称・効果を再照合する

## モバイル横オーバーフロー修正

- [x] 実機で表示された横スクロールバーと、右端で切れる操作メニューの原因を特定する
- [x] モバイルのヘッダー・操作メニュー・アイテム一覧を画面幅に収め、ページ全体の横スクロールを解消する
- [x] 390px幅の実機相当画面でアイテム・精神・プレビューを確認し、GitHub mainへ反映する
- [x] 実機アクセス時は確認用フレームを端末幅・高さへ追従させ、確認ページ由来の横・縦スクロールバーを表示しない

## モバイルUI — スクリーンショット再評価（ローカル確認のみ）

- [x] 390px実機相当のアイテム・精神・プレビュー展開状態をスクリーンショットで比較評価する
- [x] 固定下部ナビゲーションがコンパクト性・可読性・既存導線を悪化させる場合はローカルで復元する（復元不要と判断）
- [x] 再評価結果をスクリーンショットとともに報告し、GitHubへ追加反映しない

## 端末・OS・向き互換性（ローカル確認のみ）

- [x] iPhone系・Android系の小型／標準／大型画面を想定した幅・高さで、横オーバーフローと画面外操作を測定する
- [x] 縦向き・横向き、およびiOS Safari／Android Chromeで差が出やすい動的ビューポートとセーフエリアを確認する
- [x] 高さ520px以下の横向き端末では縦レールを下部2段ナビゲーションへ切り替え、全12項目を常時表示する
- [x] 下部固定レールと展開プレビューにiOSの`safe-area-inset-bottom`を反映し、旧ブラウザ用の高さフォールバックも維持する
- [x] アイテム・精神・プレビュー・操作メニューの主要導線について、各端末幅でタップ・スクロール・開閉を確認する
- [ ] 検出した互換性問題だけをローカルで修正し、GitHubへ追加反映しない
- [ ] 実iOS Safari・実Android Chromeで、ノッチ／ホームインジケータとアドレスバー展開・格納時の最終操作確認を行う

## 実機相当UI・共有HTML・共有リンクの改善

- [x] 実機に近いモバイルエミュレーションで、ヘッダー・本文・下部ナビゲーション・プレビューの重なりをスクリーンショットで特定する
- [x] 小型縦向き・横向き画面で、プレビューと主要操作が重ならず本文の利用可能領域を維持するよう修正する
- [x] 共有HTMLのサポートパッシブを「常時」と「通常効果」で段落・見出しを分離せず、効果ごとに一体で参照できる表記へ修正する
- [x] 共有HTMLのOGPと本文先頭で、人格名・HP・SAN・同期ランク・MAX状態を一目で確認できるようにする
- [x] 共有HTML先頭の人格・同期情報を一つのサマリーへ統合し、重複表示を除去する
- [x] 共有リンクを短縮し、利用者へは中間リダイレクトではなく最終共有URLを渡す設計を検討・実装する
- [x] 改善内容をスクリーンショットと共有データで検証し、GitHub mainへ反映する

## Discord共有リンク — 中間サイトを意識させない短縮体験の再検討

- [x] Discord上でLBT共有シートだと一目で分かるカード・URL・本文の要件を固定する
- [x] 短縮URL、HTTPリダイレクト、最終LBT共有ページ、個別OGPの組合せを利用者体験・無料性・管理負担で比較する
- [x] 閲覧者に中間サイトを意識させず、短い共有URLからLBTシートへ到達する推奨方式を決定する

## Discord共有リンク — 実発行テスト

- [x] 人格名・HP・SAN・同期MAXを含むテスト用共有データを発行する
- [x] 最終LBT共有URLが外部中間ページを表示せず、事前サマリーとシートを表示することを確認する
- [x] Discordに貼り付けるためのテスト用共有文とリンクを提示する

## Discord共有リンク — 共有ID中心の短縮URL

- [x] 現行LBT共有URLから復元先・予備保存先・事前サマリーを除いた場合の短縮効果を測定する
- [x] 一つの短い共有IDで主保存・予備保存・事前サマリーを復元できるURL形式を設計する
- [x] 短い共有ID URLの発行・復元・フォールバックを実装し、既存の長い共有URLも維持する
- [x] 実発行した短縮URLをDiscord用共有文とともに確認し、GitHub mainへ反映する

## 短縮共有ID — カポ最大構成の限界テスト

- [x] カポの人格データを基準に、共有テキストを長文化するE.G.O・サポート・特殊強化・所持品・編集項目を特定する
- [x] 最大構成のカポ共有データを外部主・予備保存へ実発行し、短縮共有ID URLを取得する
- [x] 公開LBT共有ページで復元内容とフォールバックを確認し、テスト用リンクを提示する
- [x] 公開ブラウザでRentry予備保存を単独取得できるよう、CORS制約を回避するフォールバック復元経路を修正する
- [x] Rentry予備保存の初回通信失敗時、共有ビューアが短い待機後に取得を再試行する
- [x] 外部共有保存の取得が応答待ちで停止しないよう、Rentry直取得と互換読み取りに時間上限を設ける

## 添付指示の確認と対応

- [x] 添付ファイルに記載された指示と目的を確認する
- [x] 指示内容がLBTへ与える影響と必要な対応範囲を整理する
- [x] 指示に基づく必要な実装または検証を実施する
- [ ] 結果を検証し、必要に応じてGitHub mainへ反映する

## ユサ人格 — 同期人格相当のオリジナル共有データ検証

- [x] 添付仕様からコンセプト行を除き、人格・派生スキル・固有バフをLBTの共有データ構造へ変換する
- [x] 同期000・MAXを含むオリジナル人格データを短縮共有IDとして実発行する
- [x] 公開共有ページで派生スキルと固有バフを含む全内容が復元されることを確認する

## 共有HTML — 所持アイテムと特殊強化の参照性

- [x] JSONの`inventory`および`customItems`を共有HTMLの所持アイテム一覧として名称・個数・効果まで表示する
- [x] 共有HTMLの特殊強化を個別に折り畳み・展開できるようにする
- [x] 共有HTMLの戦術スキルごとに通常・広域と乱射の有無を識別できるバッジを表示する
- [x] 公式アイテム・カスタムアイテム・特殊強化を含む共有リンクで表示と操作を確認し、GitHub mainへ反映する

## Base info — 共有OGP画像

- [x] Base infoで共有用OGP画像を選択・削除できるようにする
- [x] 選択画像を共有スナップショットと共有HTMLの本文・OGPメタデータへ反映する
- [x] 大きすぎる画像を共有可能な容量へ縮小し、共有・再読込・表示を検証してGitHub mainへ反映する
- [x] 画像データを外部共有データへ封入した場合の短縮URL・OGPクローラー互換性を検証し、実現可能な共有経路を採用する
- [x] 短縮URL・個別OGP画像・登録不要・請求ゼロを満たす共有方式を、保存基盤・Web配信・チャット共有の3視点で比較して採用方針を決定する（動的OGP配信なしでは個別カード画像は不可のため、請求経路を増やさない圧縮画像同梱方式を採用）
- [x] Telegraphの64KB制約に収まる画像形式・解像度・品質の最適ラインを実測し、Cloudflare非依存のOGP共有経路を選定する
- [x] 追加の動的配信基盤を使わず、Base infoの圧縮画像を短縮共有と共有HTMLへ同梱する方式を実装する
- [x] 公開利用可能な立ち絵素材で画像圧縮・短縮共有・共有HTML表示を実地検証する
- [x] 個別OGP画像を追加請求なしで表示する経路を再調査し、実現性と運用条件を整理する（静的GitHub Pages・Rentry・Telegraphのみでは個別OGP画像用HTTPSエンドポイントを提供できない）
- [x] 正方形・縦長の共有画像を共有シートの横長枠へ比率を保って表示し、過度な拡大・切り取りを防止する
- [x] 高解像度人物写真と最大級カポ構成を組み合わせ、画像圧縮後の共有容量・短縮共有復元・共有HTML表示を実地検証する
- [x] Telegraphの64KB以内に収まる画像同梱共有では、Rentry予備の失敗時にも画像を落とさず短縮共有を復元できるようにする
- [x] 全所持品・全ランクE.G.O・特殊強化全積み・通常サポート3種・死亡後サポート1種を含む最大級カポ構成の画像込み合計容量を再集計する
- [x] 個別OGP画像を返す動的配信経路を、請求リスク・運用負担・利用者登録不要性・可用性で複数比較する
- [x] Cloudflare Workers Freeで請求につながる設定と、完全に無課金運用へ固定する条件を公式仕様で確認する
- [x] Cloudflare Workers Freeのみで、共有IDごとにOGP HTMLと画像を返す無状態ゲートウェイを実装する
- [x] Paid契約・支払い方法・KV・R2・D1・Queues・Images・ドメイン購入を使わない無課金設定で配備する
- [x] 短縮共有のOGP表示・共有シート遷移・画像応答・Free設定を検証してGitHub mainへ反映する
- [x] Cloudflare連携の認証エラーとDropboxへの誤遷移を解消し、Workers Freeアカウントへのアクセスを確認する
- [x] 公式人格参照型の既存共有でも、OGPゲートウェイが公式DBから人格名・HP・SANを補完する
- [x] Cloudflare Workersの公開サブドメインを中立名へ移行し、共有URL・設定・記録から所有者由来の文字列を除去する
- [x] 現行ソース・テスト・配備記録・公開HTML・共有OGPを横断監査し、所有者由来の文字列が分析・検証経路からも露見しないことを保証する
- [x] 共有画像の圧縮見込み容量を事前判定し、上限超過時は理由を表示して共有リンク発行を停止し、再アップロードで正常に再試行できるようにする
- [x] 表示バージョンとキャッシュ参照をV65へ更新する
- [x] OGPカードの人物画像を主題に合わせて自動フレーミングし、左右余白を抑える
- [x] OGPカードの主表示を人格名へ変更し、長文名・HP・SAN・同期ランク・MAXを見切れずに表示する
- [x] 共有内容が同一なら直近の有効な共有リンクを再利用し、外部保存とURL発行の重複を防ぐ
- [x] 共有データを読み込めませんでしたとなる既存共有の復元経路・予備保存フォールバック・エラー表示を修正する
- [x] 再発した公開共有URLの「共有データを読み込めませんでした」を実URLで再現し、保存先・共有ページ・Workerの失敗経路を特定する
- [x] 既存共有と新規共有の双方で復元できるよう不具合を修正し、公開環境とGitHub mainで再検証する
- [x] 共有発行時にRentryの長いトークンを連番チャンクへ分割し、切断を防ぎつつ共有ページ・Workerで再結合する
- [x] 正常発行・主保存切断・予備保存切断・Rentry分割保存の回帰テストと公開環境確認を行い、GitHub mainへ反映する
- [x] 現行のCloudflare Workers Free共有経路について、共有発行・閲覧・OGP表示を含む日次リクエスト耐性と運用上の安全目安を算定する
- [x] 複数投稿者・多人数閲覧・Discord再プレビューを含む共有利用モデルで、Workers Freeの日次耐性と安全域を再算定する
- [x] 100〜200人規模の多人数共有を運用設計・分散システム・利用者体験の3観点で評価し、利用者の手動切替なしで無料・無管理の持続性を高める可用性改善を実装・検証する
- [x] 大人数共有時も利用者に別URLの手動選択を要求せず、Worker負荷の軽減・自動経路切替・失敗時フォールバックを自動化する
- [x] 共有の可用性改善では発行者・閲覧者に追加操作を課さず、平常時の負荷抑制と障害時の自動継続のみを採択基準にする
- [x] 無料・無管理・Discordカード・利用者無操作を維持しつつ、Worker日次受信枠への依存をさらに下げる共有アーキテクチャを比較し、最適案を実装・検証する
- [x] 自動キャッシュ・静的正規URL化による継続性向上をWorker受信・実行・外部取得・復元失敗の観点で定量評価し、追加改善の採否を決定する
- [x] Worker受信枠を減らせない通常閲覧リダイレクト分岐を撤回し、実測に基づく自動持続化策だけを残す
- [x] Workerコード更新後もCloudflare Cache設定を配備後のSettings API再適用で維持し、本番の連続共有取得でHITを実測する
- [x] 200人規模の共有発行・閲覧について、Cloudflare利用量・停止条件・利用者への影響を専門用語なしで具体例により説明する
- [x] Cloudflare入口への依存をさらに下げ、上限到達時にも共有閲覧を継続できる無料・無管理・無操作の方式を比較し、現行条件を全て満たす実装がないことを確認する
- [x] Cloudflare Worker以外で同等の個別Discord OGPカードを返す方式を、配信基盤・データ生成・Discord互換性の3観点で比較・概念検証する
- [x] 共有リンクの持続性を配信基盤・リンク/プロトコル・運用耐性の3観点で深掘りし、無料・無管理・利用者無操作・Discordカード品質を損なわない追加改善を根拠付きで選定する
- [x] 新しいエッジ配信・分散コンテンツ・静的生成・Discord連携技術から、共有リンクと個別Discordカードの持続性を飛躍的に高める実用候補を探索・検証する
- [x] Worker Cacheの`s-maxage`と`stale-while-revalidate`の競合を解消し、外部保存先障害時にもキャッシュ済み共有カードを継続表示できるようにする
- [ ] OGPを画像カード表示に限定し、Cloudflare Workers Freeの日次100,000受信枠へ到達する前にWorker到達回数を減らす無料・無管理・利用者無操作の最適化策を調査・検証する
- [x] 人格名・HP・SAN・同期・個別画像を含む現行の個別Discord OGPカードを維持したまま、Cloudflare Workers Freeの日次100,000受信枠への到達を抑える追加最適化策を調査・検証する
- [x] Worker再配備で不変共有OGPのキャッシュが空にならないよう、カードキャッシュ世代付きURLとクロスバージョンCacheを安全に導入・検証する
- [x] 共有発行・Discordカード取得・共有画像・共有ページ閲覧を分解し、現行Cloudflare Workers Freeの日次許容量を利用規模別にシミュレーションする
- [ ] 個別Discord OGPカード品質・無料・無管理・利用者無操作を保ったまま、日次許容量を高める最新技術・外部サービスを探索・検証する
- [ ] Netlify Freeの硬い無料上限を使う独立第2 OGP入口を概念実証し、共有IDの決定的振り分けでCloudflare日次受信を分散する（所有者側のNetlify接続後）
- [x] 個別Discord OGPの容量を、ユーザー数・1人あたり共有数・共有あたり閲覧数だけで理解できる簡易概算として整理する
- [x] 共有者200人を固定し、各共有リンクを開く人数中心で個別Discord OGPの日次容量を概算する
- [x] LBTの現在の実利用者数を示せるアクセス解析・公開統計・埋込計測の有無を確認する
- [x] 共有・OGP・保存経路を変更せず、LBTの匿名アクセス解析を最小限の外部タグで安全に導入する
- [x] 現行共有リンク方式の改善プランを、実装せずに機能・効果・前提条件・優先度で設計する
- [x] 個別Discord OGP・無課金・利用者無操作を維持したまま、共有リンクの耐用許容量と継続性を段階的に拡張する技術計画を設計する
- [x] 共有基盤の耐用許容量を拡張する技術を採択・保留・不採択に整理し、実測に基づく導入条件を確定する
- [x] 個別Discord OGP・無課金・利用者無操作を維持する共有基盤の耐用許容量拡張計画を、現段階の検証結果に基づき再確認する
- [x] 複数入口化に活用可能な外部サービスを、個別OGP・無料上限・無課金性・利用者無操作で選定する
- [x] ダイス個数・ダイス値に`{変数名}/10`のような変数式を設定した場合の評価結果と操作方法を確認する
- [x] d値・d数の変数名に`{変数名}`や式を入力しても二重中括弧にならず、CCFOLIAで正しく評価されるようにする
- [x] d値・d数の式入力は`floor`を使わず、`{変数名}/10`のようなCCFOLIA式をそのまま補正として出力する
- [x] d値・d数の変数名決定欄の意味を維持し、式の入力時だけCCFOLIA式として正しく反映する
- [x] d値の変数式を`xd(y-{変数名}/10)`の減算形で出力し、CCFOLIAで正しく動作させる
- [x] d値・d数の変数式で`/10`以外の除数や任意のCCFOLIA式も文字列どおりに出力されることを回帰テストで保証する
