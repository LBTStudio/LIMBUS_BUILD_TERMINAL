# v64r66 実画面検証記録

検証日: 2026-08-17

| 対象 | 確認結果 |
|---|---|
| 公式アイテム上限 | 「ガーデンの感覚遮断薬」に `上限 ×3` を表示。所持数を 1→2→3 と増加でき、3 到達時は一覧に `×3 / 3`、詳細に「上限に到達」を表示した。 |
| E.G.O 回復検索 | E.G.O検索キーワードに「回復」を表示。選択後、34件のE.G.Oが一覧に絞り込まれた。 |
| 上部バー | v64r66で主要ブランド文字、検索、操作ボタン、左ナビが初期デスクトップ表示で重なりなく描画された。 |

次段階ではブラウザの拡大率を模した縮小ビューポートでも上部バーを検証する。
