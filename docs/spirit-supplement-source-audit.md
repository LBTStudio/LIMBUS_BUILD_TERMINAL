# 精神データのサプリメント出典監査

## 確認した原典

- ユーザー提供ファイル `/home/ubuntu/upload/新リンバスTRPGサプリメント『アンロックド・シンク』.pdf`（177頁）。「精神の種類」は54〜55頁に収録されている。
- このPDFだけを精神の出典分類・効果照合の基準資料とする。

## 現在のDB状態

- `data/db.json` の `spirits` は43件で、全レコードに `source` が未設定である。
- したがって既存実装では全件がルールブック扱いとなり、サプリメントフィルタは0件となった。
- サポートパッシブには `source: "supplement"` が110件あり、精神側だけ出典分類と追加が未完了である。

## PDF照合結果

提供PDFの54〜55頁に掲載される次の7件は、既存DBに名称・価格・常時／士気／混乱効果とも収録済みだった。未収録データを創作・追加する必要はなく、各レコードへ `source: "supplement"` を付与した。

| PDF頁 | サプリメント由来の精神 |
| --- | --- |
| 54 | 快撃、潜撃、指令崩壊の危機、狂奔、時代遅れの芸術 |
| 55 | もっといい存在に成れるという希望、生き続けるという勇気 |

残る36件には `source` を付与しない。出典フィルタでは、`source: "supplement"` の7件だけをサプリメントとして表示し、それ以外はルールブックとして扱う。
