# モバイルUI視覚確認 — v64r98（ローカル）

対象: 390×812px相当の `mobile-preview.html`

## 確認した状態

| 状態 | 観察結果 |
|---|---|
| アイテム画面・プレビュー格納 | 下部ナビゲーションは2段・12項目とも画面内に表示され、表示領域外へ押し出されない。本文は下余白により最後の操作が隠れない。 |
| アイテム画面・プレビュー展開 | プレビューは下部ナビゲーションの上端で止まり、ナビゲーションを覆わない。右端タブで格納へ戻れる。 |
| 実測 | 下部レールは `top: 700px`、`bottom: 812px`。12項目の画面外要素は0、文書横幅は390px。 |

## 視覚上の評価

二段化によりラベルの文字サイズは小さいが、各タップ領域は約63×50pxを維持し、横スクロール・隠れた操作・切れたラベルよりは操作性が高い。画面密度は増したものの、既存のLBTの高密度な操作パネルとして許容できる範囲であり、この時点では「改悪」と判断する明確な視覚的根拠はない。そのため、レイアウトを戻すローカル変更は実施しない。

この文書はローカル確認の記録であり、GitHubへの追加反映を前提としない。
