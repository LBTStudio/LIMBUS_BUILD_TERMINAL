# v64r71 上部操作バー文字切れ検証

検証日: 2026-08-17

| 表示条件 | ライブラリ保存のラベル | 上部バー横オーバーフロー | 結果 |
|---|---|---|---|
| 通常表示 | 非表示（アイコンとtitleで操作を保持） | なし | 合格 |
| 150%相当 | 非表示（アイコンとtitleで操作を保持） | なし | 合格 |

中間幅や拡大表示では、長い「ライブラリ保存」の擬似ラベルを半端に残さず、アイコンだけへ切り替える。完全な操作名はhover時のtitleと「操作・保存」シート内で読める。広い表示幅では、ボタン内に全文を表示する。
